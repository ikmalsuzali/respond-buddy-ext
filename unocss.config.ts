import { defineConfig } from "unocss/vite";
import {
  presetWind,
  presetAttributify,
  presetIcons,
  transformerDirectives,
} from "unocss";

export default defineConfig({
  presets: [],
  transformers: [],
});
